# Multi-library configuration with prefix-based routing
[server]
port = 18082
workers = 1

# Prefix-based library routing
# Each library handles requests matching its prefix
# Routes are matched longest-prefix-first

[library]
/admin = examples/lib_admin.so
/api = examples/lib_api.so
